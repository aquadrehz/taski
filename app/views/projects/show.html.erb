<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @project.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @project.description %>
</p>

<p>
  <strong>Percent completed:</strong>
  <%= @project.percent_completed %>
</p>

<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>

<hr>
<h2>Task in current Project</h2>

<%= link_to "Add a new Task for this project",
            new_project_task_path(project_id: @project.id) %>